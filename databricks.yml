# This is a Databricks asset bundle definition for transact_stream_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: transact_stream_dab
  uuid: 51727eaf-ab74-4a11-a4c1-667d0624c783

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-967951729838395.15.azuredatabricks.net

    presets:
      # Set dynamic_version: true on all artifacts of type "whl".
      # This makes "bundle deploy" add a timestamp to wheel's version before uploading,
      # new wheel takes over the previous installation even if actual wheel version is unchanged.
      # See https://docs.databricks.com/aws/en/dev-tools/bundles/settings
      artifacts_dynamic_version: true

  prod:
    mode: production
    workspace:
      host: https://adb-967951729838395.15.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/joladunmoye@myseneca.ca to make sure we only have a single copy.
      root_path: /Workspace/Users/joladunmoye@myseneca.ca/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: joladunmoye@myseneca.ca
        level: CAN_MANAGE
